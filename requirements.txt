"""
Industrial-grade requirements for VigilAI v3.

Pin major versions; allow patch updates (compatible-release ~=).
Groups:
  core ML / data
  LLM / RAG
  API / serving
  dashboard
  observability
  dev / test
"""

# ── Core data & ML ─────────────────────────────────────────────────────────────
pandas~=2.2
numpy~=1.26
scipy~=1.13
scikit-learn~=1.4
xgboost~=2.0

# ── Database ───────────────────────────────────────────────────────────────────
SQLAlchemy~=2.0

# ── API ────────────────────────────────────────────────────────────────────────
fastapi~=0.110
uvicorn[standard]~=0.29
pydantic~=2.7
pydantic-settings~=2.2

# ── LLM / Agents ──────────────────────────────────────────────────────────────
langgraph~=0.0.38
langchain-core~=0.1
langchain-community~=0.0.34
langchain-google-genai~=1.0
langchain-groq~=0.1

# ── RAG / Embeddings ──────────────────────────────────────────────────────────
sentence-transformers~=2.7
faiss-cpu~=1.8

# ── MLflow experiment tracking ─────────────────────────────────────────────────
mlflow~=2.12

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit~=1.33
plotly~=5.20

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv~=1.0
requests~=2.31
tenacity~=8.3

# ── Dev / Test / Lint ─────────────────────────────────────────────────────────
pytest~=8.1
pytest-cov~=5.0
ruff~=0.4
httpx~=0.27          # async test client for FastAPI
